# UI Clone Instructions

Goal:
Recreate the current Daroda Accounting Wix site UI/UX as closely as possible using React components.

Important:
- Preserve layout, section order, and visual hierarchy
- Approximate spacing, typography, and alignment
- Do NOT redesign or simplify structure
- Use reusable components where possible

Guidelines:
- Match section structure exactly as shown in screenshots
- Use placeholder images/text where extraction is unclear
- Prioritize layout accuracy over content accuracy initially
- Build components for repeated patterns (CTA sections, headings, cards)

Do NOT:
- invent new sections
- significantly change layout
- over-engineer styling